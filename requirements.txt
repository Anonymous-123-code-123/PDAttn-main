# PDAttn — Python dependencies (from imports across PDAttn/, data_tools/, tools/, task/)
#
# ---------------------------------------------------------------------------
# Install (recommended)
# ---------------------------------------------------------------------------
#   cd PDAttn-main
#   python -m venv .venv
#   .venv\Scripts\activate          # Windows
#   #	source .venv/bin/activate   # Linux / macOS
#
#   For GPU: install a CUDA build of PyTorch first, then the rest:
#     pip install torch --index-url https://download.pytorch.org/whl/cu124
#     # or follow: https://pytorch.org/get-started/locally/
#   Then:
#     pip install -r requirements.txt
#
#   CPU-only quick path (often enough for debugging):
#     pip install -r requirements.txt
#
# If ``torch-geometric`` fails to install, use the wheel index matching your
# PyTorch and CUDA version: https://pytorch-geometric.readthedocs.io/en/latest/install/installation.html
#
# Suggested: Python >= 3.9
# ---------------------------------------------------------------------------

# Deep learning & graph neural networks
torch>=2.0.0,<3.0.0
torch-geometric>=2.4.0

# Numerics & tables
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0

# Single-cell / AnnData pipeline (data_tools, task/data_load.py)
scanpy>=1.9.0
anndata>=0.10.0

# Graphs & I/O helpers
networkx>=3.0
tqdm>=4.65.0
requests>=2.28.0
